<div class="favorite-button">
	<%= form_for(current_user.favorites.find_by(photo_id: @photo.id),
	             html: { method: :delete },
	             remote: true) do |f| %>
	  <%= f.submit "Unfavorite", class: "btn" %>
	<% end %>
</div>